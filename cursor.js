var showCursor = false
var speed = 500

setInterval(() => {
if(!showCursor) {
    document.getElementById('cursor').sty
    showCursor = true
}else {
    document.getElementById('cursor').sty
    showCursor = false
}
}, speed)
